
/**
 * Client Functionality
 * This application can act as a server and a client.
 * Thus, peers reconnecting to the network act as clients, joining the 'servers' of the connected peers.
 * This connected peer will also host a server of it's own, described in server.rs
 * 
 * For all intents and purposes, the application acting as a client is remarkably similar to 
 */

struct Client {
	
}